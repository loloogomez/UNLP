program ejercicio9;

var
	c: char;
	n: integer;
	cuenta: char;

BEGIN
	write ('ingresar + o -: ');
	readln (c);
	if ((c = '+') or (c = '-')) then
	begin
		while (n <> 0) do
		begin
			write ('ingresar numero: ')
			readln (n)
			cuenta:= 
		end;
	end
	else
		writeln ('error');
	writeln (cuenta);
	
END.

